export interface PostMetadata {
    title: string,
    date: Date
}

export interface ProjectMetadata {
    title: string,
    date: Date
}

export interface ResearchMetadata {
    title: string,
    date: Date
}